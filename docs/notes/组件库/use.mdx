# 使用

```ts
import { createApp } from "vue";
import BetterUI from "better-design-vue";
import "better-design-vue/dist/index.min.css";

import App from "./App.vue";

const app = createApp(App);
app.use(BetterUI);
app.mount("#app");
```

