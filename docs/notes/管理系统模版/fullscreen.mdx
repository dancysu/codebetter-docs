# 全屏模式

定义一个名为 `fullScreen` 的函数,用于实现页面的全屏显示和退出全屏功能。

```ts
const fullScreen = () => {
  // 检查当前是否处于全屏模式
  if (document.fullscreenElement) {
    // 如果是全屏模式,退出全屏
    document.exitFullscreen()
  } else {
    // 如果不是全屏模式,请求进入全屏
    document.documentElement.requestFullscreen()
  }
}
```

